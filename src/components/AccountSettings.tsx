import { View, Text, StyleSheet } from "react-native";
import {sc, ms, vs} from './../constants/responsive';
import { ShieldUser, Trash2 } from "lucide-react-native";
export default function AccountSettings() {
  return (
    <View>
        
    </View>
  );
}
